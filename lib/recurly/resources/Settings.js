'use strict';

/**
 * Settings
 * @typedef {Object} Settings
 * @prop {Array.<string>} accepted_currencies
 * @prop {string} billing_address_requirement - - full:      Full Address (Street, City, State, Postal Code and Country) - streetzip: Street and Postal Code only - zip:       Postal Code only - none:      No Address 
 * @prop {string} default_currency - The default 3-letter ISO 4217 currency code.
 */
class Settings {}

module.exports = Settings
