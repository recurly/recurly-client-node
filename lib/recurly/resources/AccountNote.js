'use strict'

/**
 * AccountNote
 * @typedef {Object} AccountNote
 * @prop {string} account_id
 * @prop {datetime} created_at
 * @prop {string} id
 * @prop {string} message
 * @prop {User} user
 */
class AccountNote {}

module.exports = AccountNote
