'use strict'

/**
 * AccountNote
 * @typedef {Object} AccountNote
 * @prop {string} accountId
 * @prop {datetime} createdAt
 * @prop {string} id
 * @prop {string} message
 * @prop {User} user
 */
class AccountNote {}

module.exports = AccountNote
