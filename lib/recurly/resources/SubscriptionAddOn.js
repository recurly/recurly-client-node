'use strict'

/**
 * SubscriptionAddOn
 * @typedef {Object} SubscriptionAddOn
 * @prop {AddOnMini} add_on
 * @prop {datetime} created_at - Created at
 * @prop {datetime} expired_at - Expired at
 * @prop {string} id - Subscription Add-on ID
 * @prop {number} quantity - Add-on quantity
 * @prop {string} subscription_id - Subscription ID
 * @prop {number} unit_amount - This is priced in the subscription's currency.
 * @prop {datetime} updated_at - Updated at
 */
class SubscriptionAddOn {}

module.exports = SubscriptionAddOn
