'use strict'

/**
 * User
 * @typedef {Object} User
 * @prop {datetime} created_at
 * @prop {datetime} deleted_at
 * @prop {string} email
 * @prop {string} first_name
 * @prop {string} id
 * @prop {string} last_name
 * @prop {string} time_zone
 */
class User {}

module.exports = User
